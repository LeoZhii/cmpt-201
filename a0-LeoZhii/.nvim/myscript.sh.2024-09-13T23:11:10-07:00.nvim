#!/bin/zsh
# My first script!
# Leo Zhi
# September 13, 2024

name='Leo'
echo Hello $name, These are the files in your current directory:
ls

echo The number of files in the directory is:
ls -f

echo The number of directories in the directory is:
ls -d

echo The biggest file is:
ls -b

echo The most recently modified/created file is:


echo a list of people who own files in the directory is:
