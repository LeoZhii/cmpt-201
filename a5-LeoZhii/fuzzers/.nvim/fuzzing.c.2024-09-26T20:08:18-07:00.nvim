#include <stddef.h>
#include <stdint.h>

int LLVMFuzzerTestOneInput(const uint8_t *data, size_t size) {
  uint8_t input[sizeof(inte32_t)] = {0}; // Making sure we initialize every byte to zero

  int32_t ret = 0;

  if (num < 0) {
    ret = -num;
  } else {
    ret = num;
  }

  return ret;
}
